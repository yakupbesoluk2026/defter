<!--
  Bu blok, `npx cap add android` ile oluşturulan
  android/app/src/main/AndroidManifest.xml dosyasındaki
  <application> ... </application> etiketinin İÇİNE,
  mevcut <activity> bloğundan sonra eklenmelidir.
-->
<receiver
    android:name=".DefterWidgetProvider"
    android:exported="true">
    <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
    </intent-filter>
    <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/widget_defter_info" />
</receiver>
